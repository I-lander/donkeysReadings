# Server (LLM API)
OPENAI_API_KEY=
PORT=3001

# Client: base URL of the reading API.
# Empty in dev (Vite proxies /api to localhost:3001).
# For the Android build, set the deployed server URL, e.g. https://api.example.com
VITE_API_URL=
